%include './Numeral.lcs'

# Pair

PAIR   := \xy.  (\z. z x y)
FIRST  := \p. p (\xy. x)
SECOND := \p. p (\xy. y)

# Values

CONS := PAIR
NIL  := FALSE

# Operators

HEAD := FIRST
TAIL := SECOND

TAIL (TAIL (CONS 1 (CONS 2 3)))